# NOTES.md

CLAUDE.md: I have added commands/conventions/architecture. Commands section include commands for starting the API with or without auto-reload, running tests and running ESLint.

Permisisons: Deny for force push to to not allow forced overwrite, deny for reading environment variables, because this could be sensitive. Allow npm test and ask git push