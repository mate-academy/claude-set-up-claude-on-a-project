Modify CLAUDE.md that generated from /init
- Remove from convention -> project is ESLint script (obviously)
- Revise convention for config read from .env

Permission setting:
- Add deny rule for "rm -rf" -> This can't be undone