# Notes on Claude Code Setup
## CLAUDE.md Choices
I included [description, commands, conventions, architecture] because [reason].
I left out [things you cut] because [they were obvious/sensitive/not useful].
## Permission Rules
I added [allow rule] to [reason].
I added [deny rule] because without it, [what could go wrong].
## Verification
Confirmed via /memory that CLAUDE.md loads correctly.
Confirmed via /permissions that rules are active.

I create a new pull request to try GitHub auto test.