The CLAUDE.md file contains three sections: Architecture, Commands and Conventions.
They detail how the project should be developed when Claude Code is being used.
Note that excludes for git are included in the .gitignore file
Check Claude for allows: testing, ask: push to git, deny: force push to git
Without a deny force push any untested, undocumented features could get into a build