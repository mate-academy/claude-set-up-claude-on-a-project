I have put in the CLAUDE.md information about project, commands it uses, conventions it follows and architecture.
I have added deny, allow and ask rules. Without deny rule the credentials could be stored in public Git repo and everybody could read them.
