For the CLAUDE.md: the initial file generated via /init was good enough. I just removed some extraenous text in the introduction.

I added the allow rule for allowing claude to run the tests without asking for my permission when it deems it necessary. 
As for deny, I explicitely denied push force on github
