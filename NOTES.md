removed header comment:
A small Express API (users + health endpoints) that serves as the starter project for the Claude Code course — used to configure Claude Code itself, not to build out app features.

removed from commands:
CI (`.github/workflows/ci.yml`) runs `npm install`, `npm run lint`, and `npm test` on every push/PR — keep both passing.

/memory shows the CLAUDE.md loaded 
/permissions shows the rules as specified 