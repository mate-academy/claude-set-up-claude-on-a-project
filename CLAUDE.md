# This project creates a web server.

## Commands
npm run dev
npm test
npm run lint

## Conventions
Use X not Y

## Architecture
the server.js entry point.
one route file per resource in routes/.
data access through db/store.js.