# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Purpose

This project sets up Claude Code in a Node.js environment to automate code analysis and refactoring.

## Commands

- `npm run dev` — run the app in development
- `npm test` — run tests
- `npm run lint` — lint the code

## Status

This repository currently has no source files or `package.json` yet — the commands above are the intended scripts for this project but are not yet backed by an actual Node.js setup. There is no build system, test suite, or documented architecture in place.

## Working here

- The commands above are intended, not yet functional — verify `package.json` exists and defines these scripts before relying on them.
- Once real project files are added (package.json, source structure, etc.), update this file to reflect actual architecture.
